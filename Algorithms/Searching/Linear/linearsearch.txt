Linear Search: look at every element and check if it's the value we want

Given an array, the simplest way to search for a value is to look at every element in the array and check if it's the value we want

JavaScript has linear search methods on array:
  indexOf
  includes
  find
  findIndex

  Each of the above methods go through each item